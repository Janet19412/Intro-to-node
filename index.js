//jshint esversion:6
// the comment will tell the linter that I'm actually using ES Version 6. because the const is only available in ES6.

var supervillains = require("supervillains");

supervillainsName = supervillains.random();

console.log(supervillainsName);
